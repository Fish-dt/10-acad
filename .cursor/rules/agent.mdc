---
description: Tenx Agent Rules
globs: **/*
---
# Rules
- Use a `<thought>` block before every action.
- Always use the `tenxfeedbackanalytics` tool to log progress.
- Prioritize modular code over monolithic scripts.